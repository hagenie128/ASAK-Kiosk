/*
 * 결제수단 목록 UI (SCR-007) — 자리표시자
 *
 * mock rows: paymentMethods.data[]
 *   methodId, name, description, isActive, isMaintenance, sortOrder
 * Props 후보: methods, selectedId, disabled?, onSelect(methodId)
 * PaymentPage 로컬 METHODS 상수 대체용
 * 표: public/mocks/README.md §1
 */
export default function PaymentMethodList() {
  return null;
}
