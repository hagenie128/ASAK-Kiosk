# ASAK 작업 기록 템플릿

> **Git:** [`docs/guides/README.md`](README.md) — 가이드 읽기 순서 **03**

이 템플릿은 **기능·이슈·PR 단위**로 작업 기여도, AI 활용, 디버깅 이력, 개선사항을 깊게 남기기 위한 문서입니다.

권장 저장 위치: `worklog/entries/`  
권장 파일명 예시:

- `2026-07-02-menu-option-ui.md`
- `2026-07-02-order-api-bugfix.md`

---

## 일일 워크로그와의 관계

| 구분 | 일일 (`worklog/daily/{이름}/`) | 상세 (`worklog/entries/{이름}/` + 이 템플릿) |
|---|---|---|
| **단위** | 하루 (팀·개인 요약) | 기능·이슈·PR 하나 |
| **목적** | 오늘 뭘 했는지 팀 캘린더·Notion에 공유 | 구현·디버깅·AI·면접용 깊은 기록 |
| **분량** | 표 1줄 + **미니 카드** (섹션당 1~2줄) | 아래 **12섹션** 전체 |
| **공통 어휘** | 작업 목적 · 직접 구현 · AI 도움 · 이슈 · 검증 · 포트폴리오 요약 | 동일 이름의 본문 섹션 (§2·§3·§5·§6·§7·§10·§11) |

**권장 흐름**

1. 하루 중 — `entries/`에 이 템플릿으로 상세 작성 (또는 작업 끝날 때 한꺼번에)
2. 퇴근 전 — `daily/{이름}/YYYY-MM-DD.md` **오늘 요약** 표에 한 줄 + **오늘 작업** 미니 카드에 핵심만 적고 `entries/{이름}/` 링크
3. Notion — `sync_daily_to_notion.py`가 **표 행**만 캘린더 DB에 올림 (미니 카드·상세는 Git 정본)

예시: [`04-sample-work-log-example.md`](04-sample-work-log-example.md) (상세 12섹션) · daily 연결 예시는 [`worklog/guide-team-daily.md`](../../worklog/guide-team-daily.md) 「daily ↔ entries 예시」

---

## 1. 기본 정보

- 작업 날짜:
- 담당자:
- 저장소:
- 브랜치:
- 관련 이슈/PR:
- 작업 유형: `feature` / `fix` / `docs` / `refactor`

## 2. 작업 목적

- 이번 작업에서 해결하려고 한 문제:
- 기대 결과:

## 3. 직접 구현 영역

직접 생각하고 구현한 부분을 구체적으로 적습니다.

- 예:
  - 옵션 선택 UI 상태 구조 설계
  - 장바구니 합계 계산 로직 구현
  - API 엔드포인트 및 서비스 메서드 작성

## 4. 구현 로직 / 적용한 방식

이번 작업에서 어떤 로직, 구조, 흐름을 사용했는지 적습니다.

- 어떤 로직을 사용했는지:
- 왜 그 방식을 선택했는지:
- 데이터 흐름 또는 처리 순서:
- 핵심 조건문 / 상태 변경 / 예외 처리 방식:

예:

- 어떤 로직을 사용했는지: 옵션 선택값을 배열로 관리하고, 선택 상태가 바뀔 때마다 총액을 다시 계산하는 방식 사용
- 왜 그 방식을 선택했는지: 컴포넌트 수가 많지 않고, 현재 팀 규모에서는 전역 상태관리보다 단순 상태 조합이 더 빠르기 때문
- 데이터 흐름 또는 처리 순서:
  - 옵션 선택
  - 상태 업데이트
  - 가격 재계산
  - 장바구니 반영
- 핵심 조건문 / 상태 변경 / 예외 처리 방식:
  - 필수 옵션 미선택 시 버튼 비활성화
  - 수량 변경 시 음수 방지 처리
## 5. AI 도움 영역

AI가 실제로 도움 준 부분만 적습니다.

- 사용한 AI 도구:
- 어떤 질문/요청을 했는지:
- AI가 도움 준 내용:
- 그대로 사용한 부분:
- 수정해서 사용한 부분:

예:

- 사용한 AI 도구: Cursor
- 질문: "Spring Boot 3.3 기준으로 주문 생성 API 구조를 제안해줘"
- 도움 내용: Controller-Service-DTO 기본 구조 초안 제안
- 그대로 사용한 부분: 예외 응답 형식 아이디어
- 수정해서 사용한 부분: DTO 필드, 검증 로직, DB 연관관계

## 6. 발생 이슈

작업 중 실제로 겪은 문제를 적습니다.

- 이슈 1:
  - 증상:
  - 원인:
  - 해결:

- 이슈 2:
  - 증상:
  - 원인:
  - 해결:

## 7. 디버깅 기록

나중에 같은 문제를 다시 만났을 때 바로 도움이 되도록 적습니다.

- 확인한 로그/에러 메시지:
- 의심했던 지점:
- 실제 원인:
- 다시 같은 문제가 생기면 먼저 볼 파일/명령어:

예:

- 확인한 로그/에러 메시지: `NullPointerException at OrderService.createOrder`
- 의심했던 지점: 요청 DTO null, 엔티티 매핑, 옵션 리스트 파싱
- 실제 원인: 프론트에서 옵션 배열이 비어 있을 때 `null`로 전송됨
- 다시 같은 문제가 생기면 먼저 볼 파일/명령어:
  - `OrderController`
  - `OrderRequest`
  - 브라우저 Network 탭 요청 body

## 8. 이번 작업에서 배운 점

구현하거나 디버깅하면서 새로 이해한 개념, 익숙해진 방식, 다음에 써먹을 수 있는 포인트를 적습니다.

- 새로 배운 기술/개념:
- 이번에 이해가 선명해진 부분:
- 다음 작업에 적용해보고 싶은 점:

예:

- 새로 배운 기술/개념: React에서 상태를 직접 변경하지 않고 새 배열로 교체해야 화면이 안정적으로 다시 렌더링된다는 점
- 이번에 이해가 선명해진 부분: Spring DTO 검증과 Controller 예외 응답 흐름
- 다음 작업에 적용해보고 싶은 점: 주문/결제 단계에서도 같은 검증 흐름 재사용

## 9. 개선사항 / TODO

- 아직 임시 처리한 부분:
- 다음 스프린트에서 개선할 부분:
- 성능/구조적으로 아쉬운 부분:

## 10. 검증 내용

- 실행한 명령어:
- 테스트한 시나리오:
- 확인 결과:

예:

- 실행한 명령어:
  - `python build_viewer.py`
  - `./gradlew test`
- 테스트한 시나리오:
  - 메뉴 선택
  - 옵션 변경
  - 장바구니 담기
- 확인 결과:
  - 정상 동작
  - 가격 계산 정상
  - 옵션 미선택 예외 처리 필요

## 11. 포트폴리오용 요약

한두 줄로 정리합니다.

- 예:
  - React 기반 키오스크 메뉴 옵션 선택 화면을 구현하고, 상태 변경에 따른 가격 계산 로직을 직접 설계했다.
  - Spring Boot 주문 API 디버깅 과정에서 요청 DTO null 처리 문제를 해결하고, 예외 응답 구조를 정리했다.

## 12. 첨부하면 좋은 자료

- 스크린샷
- 에러 로그
- 테스트 영상
- PR 링크
- 관련 피그마/노션 링크
